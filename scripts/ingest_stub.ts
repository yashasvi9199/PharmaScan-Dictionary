// scripts/ingest_stub.ts
// Placeholder ingest script. In the future, this will fetch external datasets,
// normalize them, and output cleaned CSV files inside /data.
// For now, it simply prints a message so CI can verify the script runs.

console.log("Ingest stub executed. No data processing implemented yet.");
